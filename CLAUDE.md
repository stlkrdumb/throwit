@AGENTS.md

## Agent Rules

### Never run the dev server
Do NOT start or manage any development servers (`npm run dev`, `next dev`, `bun run dev`, etc.). The human runs these themselves. Only read files, write/edit code, and execute non-server commands (tsc, lint, build — but not `dev`).
